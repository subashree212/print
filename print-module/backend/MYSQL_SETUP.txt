# If switching to MySQL, add this dependency to pom.xml
# (and remove or keep H2 for tests):

# In pom.xml <dependencies> section, add:
# <dependency>
#     <groupId>com.mysql</groupId>
#     <artifactId>mysql-connector-j</artifactId>
#     <scope>runtime</scope>
# </dependency>

# Then run with MySQL profile:
# ./mvnw spring-boot:run -Dspring-boot.run.profiles=mysql
